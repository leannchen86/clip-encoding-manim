Scene 1: CLIP Encoders & Shared Embedding Space
The scene opens with two parallel pipelines, left for text and right for image. On the text side, show a short phrase like “a photo of a dog” or “David” rendered as text. Animate the phrase being broken into token blocks, which pass into a stylized transformer-like “Text Encoder” box built from stacked layers. On the image side, show a clean image thumbnail. Overlay a patch grid, then animate those patches feeding into a matching “Image Encoder” box. Each encoder emits a vertical embedding vector with glowing numeric components or feature bars. Both embeddings then move inward and get projected into a shared 2D embedding plane in the center. The two corresponding points land near each other and glow. A dotted line appears between them with the label “high cosine similarity.” End by populating the space with several image-text pairs forming loose semantic neighborhoods. Avoid making clusters too clean or too perfect.

Scene 2: Contrastive Learning & the NxN Similarity Matrix
The scene starts with N image thumbnails arranged in a row at the top of the screen, and N corresponding text prompts arranged in a column on the left — forming the axes of a grid. Animate each cell of the grid filling in as a dot product is "computed" — represented by two vectors briefly appearing, multiplying, and collapsing into a scalar value that populates the cell. Diagonal cells light up in a warm color (high similarity, correct pairs), while off-diagonal cells cool to dark blue (low similarity, incorrect pairs). Add a subtitle: "Diagonal = matched pairs -> push similarity HIGH. Off-diagonal = mismatched -> push similarity LOW." Then overlay a softmax normalization step: each row of the matrix briefly flashes as a bar chart, normalizes, and snaps back into the matrix. Show two loss arrows: one labeled L_image sweeping across rows, another labeled L_text sweeping down columns, then averaging into a single L_CLIP label at the center.

Scene 3: L2 Normalization & Cosine Similarity on a Hypersphere
Start in 2D with a raw embedding vector from the origin whose tip lies outside the unit circle. Animate the vector shrinking or stretching onto the unit circle while keeping its angle fixed. Label the formula briefly: normalized vector equals original vector divided by its norm. Then transition into a 3D sphere or a strong 2.5D illusion of a sphere, with several normalized embeddings shown as glowing points on the surface. Highlight two points and draw their angle from the center, labeling theta. Then show cosine similarity as depending only on that angle. Animate two points moving closer in direction and then farther apart. Keep one line of plain-language text: “After normalization, direction matters more than raw magnitude.”

Scene 4: CLIP vs. VLM Architecture Comparison
Show CLIP's architecture on the left — two separate encoders, similarity score output. On the right, show a vision-language model like Qwen2.5-VL: image encoder feeds into a language model that generates text autoregressively. Highlight the key difference: CLIP compares, VLMs generate. Animate a face image going through both pipelines — CLIP outputs a similarity ranking over names, while the VLM outputs a sentence like “This person looks like a David.” End with one short caption: “For this project, CLIP-style embeddings are the more useful interface.” Keep the comparison structural and visual. Do not turn this into a benchmark table scene.

Scene 5: UMAP Projection and Projection Caveats
Begin with a chaotic cloud labeled “High-dimensional embedding space,” without overcommitting to a literal “768 dimensions” if that makes the scene too awkward. Show a local neighborhood around one point, with edges to nearby neighbors, to establish that UMAP starts from neighborhood relationships. Then animate the cloud being projected into a 2D scatter plot. The final 2D plot should show partial structure: some local neighborhoods remain coherent, some groups are somewhat separated, but the global layout should not look perfectly faithful or mathematically exact. Use a label like: “UMAP preserves local neighborhoods and can reveal broader structure.” As a brief closing beat, optionally split the screen and show a t-SNE version beside it, with a note that t-SNE often makes tighter local islands while UMAP is better for broader relative structure. Do not claim either projection is a literal map of the full geometry.

Scene 6: Gender Shortcut Reveal
Show a 2D feature space with David points on one side and Mary points on the other — clean separation, with a nearly perfect decision line between them. Label the result as very high accuracy. Then drop in Daniel and William points as new test examples. They all land on the David side. The decision line should relabel from “David / Mary” to “Male / Female.” Zoom out and add a caption: “The model found a shortcut.” The point of the scene is that the classifier appears to solve the task, but is really exploiting gender signal instead of name-specific signal.

Scene 7: MLP Decision Boundaries on Frozen CLIP Features
Start with a 2D scatter plot of two interleaved same-gender classes, such as David and Thomas. A single straight line tries to separate them but fails — label it “linear classifier: insufficient.” Fade in a small MLP diagram on the side: input layer -> hidden layers -> output layer, with a simple ReLU bend graphic if useful. As each layer processes the data, animate the feature space warping via a smooth transformation, until in the final latent space the two classes become more separable. A cleaner decision boundary appears in the transformed space, then maps back to the original space as a curved non-linear boundary. End with a short caption: “Frozen CLIP features contain weak signal, and the probe shapes a better boundary.” Do not make the final separation look perfect.

Scene 8: MMD — Small but Measurable Distribution Difference
Create two overlapping 2D point clouds: blue points labeled David and orange points labeled Thomas. The two clouds should overlap heavily, with only a slight shift in center. Show the two clouds appearing one after another. Add a top caption: “Are these two distributions measurably different?” Select three blue sample points and three orange sample points. Draw faint radial kernel circles around each selected point. Show pairwise similarity lines for blue-to-blue, orange-to-orange, and blue-to-orange. Then compress the visual into three summary bars: within David, within Thomas, and across names. Morph those bars into one scalar label: “MMD > 0.” Add a bottom caption: “Small but measurable distribution difference.” Do not make the two clouds look cleanly separated. The whole point is that they overlap a lot.

Scene 9: Silhouette Score with Weak Separation
Use the same two-class scatter plot as Scene 8 so the visual continuity is obvious. Highlight one blue point. Draw lines from it to several nearby blue points and label the average as a = within-class distance. Then draw lines from the same point to the nearest orange neighborhood and label that average as b = nearest other-class distance. Show the silhouette formula in small text: s = (b - a) / max(a, b). Immediately after, place a number line from -1 to +1. Animate a marker landing slightly above zero, around 0.08 or 0.12. Add a bottom caption: “Weak separation, not clean clustering.” Do not end with neatly separated clusters.

Scene 10: Convergence Test and Signal Ceiling
Open with an animated line chart. X-axis: number of training images per name. Y-axis: accuracy. Start at 0 — the line sits at the random baseline, shown as a dotted horizontal line. As the count ticks up through values like 5, 10, 50, 100, 500, 1000, 5000, and 7500, the accuracy line climbs quickly at first and then begins to flatten. Annotate three moments: at 5 images (“signal appears”), at 500 (“gains slow”), and at 7,500 (“plateau”). End by drawing the asymptote and labeling it “signal ceiling.” Keep the curve smooth and monotone, more like a diminishing-returns learning curve than a noisy training trace.

Scene 11: Softmax Probability Budget and Crowded-Out Names
Create a bar chart with six names: William, Jessica, Sarah, Thomas, Laura, and Robert. At time step 1, all bars are moderate and sum to 100%. Then animate a training progression over four steps: William, Jessica, and Sarah rise, Thomas and Laura shift slightly, and Robert steadily shrinks. Show a softmax label at the top: “Probabilities must sum to 1.” Add a bottom caption: “As the model gets more confident about some names, harder names can get crowded out.” Final frame: Robert is very small while the other bars dominate. Avoid wording like “the model learns not to predict Robert.” Use “Robert gets crowded out.”

Scene 12: Prediction Distribution, Confusion Matrix, and Over-Predicted Names
Start with a balanced validation set shown as a row of equal-size name blocks or a bar chart where every name has the same true share. Label this “True distribution.” Then introduce a second chart labeled “Predicted distribution.” Animate a few names such as Todd, Kyle, and Stuart growing much larger than the others, while many names stay very small. Add a caption: “Accuracy alone does not show where wrong guesses go.” Then compress this view into a confusion matrix or a highlighted set of confusion columns, so the viewer can see that one or two names are receiving far too many wrong predictions. Highlight one over-predicted name and annotate that it appears about 10x more often than expected. End with both the balanced truth and skewed predictions still visible in some form. Keep the predicted distribution visibly skewed, not subtle.

Scene 13: Tight Clusters Become Attractors
Show a 2D embedding space with two named clusters: one small and tight, one larger and more diffuse. Then add several ambiguous query points near the boundary between regions. For each query point, briefly show score arrows or similarity lines pointing toward both classes. The tight cluster should win more often, even for nearby non-matching points. Then fade in soft decision regions around both clusters, making the tight-cluster region visibly too large. Add a caption: “Tight clusters can attract uncertain predictions.” Do not present this as a physics-style sink or energy minimum. Present it as an oversized decision region in feature space.

Scene 14: Truncate Before Shuffle vs. Shuffle Before Slice
Split the screen into two pipelines. On the left, show a long ordered list of embeddings for one class. First slice off the first N items, then shuffle them. Label this path “truncate first.” On the right, take the full list, shuffle it, then slice out train and validation subsets. Label this path “shuffle first.” Use subtle color variation in the original ordered list so the left-side prefix looks biased or repetitive, while the right side looks mixed. Then reveal a result card below each side, with the fixed pipeline clearly outperforming the buggy one. Add a bottom caption: “A sampling bug can hide the real model behavior.” The point should be the order-of-operations mistake, not detailed implementation.

Scene 15: BatchNorm vs. Width
Show two probe architectures side by side. On the left, a wider MLP with many neurons but no normalization. On the right, a narrower MLP with BatchNorm blocks between layers. Animate activations flowing through both networks. On the no-BN side, some feature bars should become too large while others nearly disappear, making the internal scales feel uneven. On the BN side, the activations should get re-centered and re-scaled so the flow remains balanced. Then show a small result comparison below: fewer parameters on the BatchNorm side, but better final performance. Add a caption: “Normalization matters more than brute-force width.” Avoid making the wide model look broken; it should look plausible but less stable.

Scene 16: Full Batch vs. Mini-Batch
Open with two training panels side by side. Both optimize the same probe on the same frozen embeddings. On the left, mini-batch training takes many slightly noisy steps, shown as a jittery path on a simple loss plot or optimization diagram. On the right, full-batch training takes fewer but cleaner steps in a steadier direction. Then replace the abstract diagram with a concrete note: “All embeddings fit in memory.” Add a result comparison showing full-batch slightly outperforming mini-batch in this specific setup. End with a caption: “When the problem is already clean, extra gradient noise may not help.” Do not imply that full-batch is always better in general.

Scene 17: Frozen CLIP + MLP vs. Fine-Tuned CLIP
Split screen. On the left, show CLIP as a locked or frozen encoder, with only a small MLP head highlighted as trainable. On the right, show the full CLIP stack unfrozen, with weights updating throughout the encoder. Animate training on both sides. Beneath each model, show two compact indicators: train accuracy and validation accuracy. The fine-tuned model’s train accuracy should rise much faster, but its validation result should stall or slip relative to the frozen-probe setup. End with a caption like: “More trainable parameters do not automatically improve generalization.” Keep the comparison about overfitting rather than about raw training speed.

Scene 18: Held-Out OOD and Abstain Policy
Create three evaluation buckets: in-vocab validation, held-out names, and live user uploads. Show the model behaving confidently on in-vocab data, then becoming less reliable on held-out and live inputs. Next, introduce two signals for each example: probe prediction and centroid prediction. For some uncertain examples, animate those two disagreeing. Then add a simple decision gate: if confidence is low or the two methods disagree, abstain instead of forcing a guess. End with a before-and-after comparison: fewer bad public predictions, but some inputs now return “not sure.” Add a caption: “For a public demo, abstaining can be better than guessing.” Keep the tradeoff explicit: better behavior, lower coverage.
